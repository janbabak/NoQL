name: Validate backend version

on:
  workflow_call:
    outputs:
      backend_changed:
        description: 'True if backend code has changed on this branch.'
        value: ${{ jobs.detect-backend-changes.outputs.backend_changed }}

jobs:
  # Detect changes in the /backend folder (ignoring /backend/docker/plotService.Dockerfile)
  detect-backend-changes:
    runs-on: ubuntu-latest
    outputs:
      backend_changed: ${{ steps.detect.outputs.changed }}

    steps:
      - uses: actions/checkout@v4

      - name: Fetch main branch
        run: git fetch origin main

      - name: Detect backend changes
        id: detect
        run: |
          if git diff --name-only origin/main ${{ github.sha }} \
            | grep '^backend/' \
            | grep -v '^backend/docker/plotService.Dockerfile$' \
            | grep -q .; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

  # Validate backend version has been increased when backend changes have been detected
  validate-backend-version:
    runs-on: ubuntu-latest
    needs: detect-backend-changes
    if: needs.detect-backend-changes.outputs.backend_changed == 'true'

    steps:
      - uses: actions/checkout@v4

      - name: Fetch main branch
        run: git fetch origin main

      - name: Get current backend version
        id: pr_version
        run: |
          PR_VERSION=$(grep '^version' backend/build.gradle \
            | awk -F= '{gsub(/[ '\'',]/,"",$2); print $2}')
          echo "PR_VERSION=$PR_VERSION" >> $GITHUB_OUTPUT

      - name: Get main backend version
        id: main_version
        run: |
          MAIN_VERSION=$(git show origin/main:backend/build.gradle \
            | grep '^version' \
            | awk -F= '{gsub(/[ '\'',]/,"",$2); print $2}')
          echo "MAIN_VERSION=$MAIN_VERSION" >> $GITHUB_OUTPUT

      - name: Check semantic version increment
        run: |
          ./ci/scripts/check-semantic-versioning.sh \
            "${{ steps.pr_version.outputs.PR_VERSION }}" \
            "${{ steps.main_version.outputs.MAIN_VERSION }}"